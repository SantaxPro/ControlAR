import {View, Text, StyleSheet, StatusBar, FlatList} from "react-native";
import theme from "../UI/theme";
import {useState} from "react";

export default function StudentAssistCard() {
    
    return(
        <View >
            <Text>StudentAssistCard</Text>
        </View>

    )
}